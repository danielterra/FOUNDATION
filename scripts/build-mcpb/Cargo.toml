[package]
name = "build-mcpb"
version = "0.1.0"
edition = "2024"

[dependencies]
zip = { version = "2.2", default-features = false, features = ["deflate"] }
serde_json = "1.0"
anyhow = "1.0"
walkdir = "2.5"
