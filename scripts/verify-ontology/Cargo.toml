[package]
name = "verify-ontology"
version = "0.1.0"
edition = "2024"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
dirs = "5.0"
foundation_app = { path = "../../src-tauri", package = "FOUNDATION" }
